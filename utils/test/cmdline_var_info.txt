do_fork              d      bool      true    "forks off grandchild for each client"
become_daemon        b      bool      true    "uses setuid, so only succeeds as root"
port                 p      int       8088    "port"
