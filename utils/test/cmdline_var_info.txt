do_fork              bool      true    "forks off grandchild for each client"
become_daemon        bool      true    "uses setuid, so only succeeds as root"
port                 int       8088    "port"
