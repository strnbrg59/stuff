#!/usr/local/bin/expect

proc poisson_order { } {
	spawn aqc;
	expect "knickname:";     # It doesn't see "knickname: " (w/ space)
	send "poisson\r";
	expect "password:";
	send "shbolet\r";
	expect "q to quit. >";
	send "T\r";
} ## proc poisson_order
