# We say "EXTRA" because otherwise the list of files has to be static.
# On the other hand, maybe this is why "make clean" doesn't get rid of
# the hdf5 files.
EXTRA_ppm_DATA = $(wildcard *.ppm.gz)
EXTRA_ppmdir = $(pkgdatadir)/data

EXTRA_DIST = *.ppm.gz
